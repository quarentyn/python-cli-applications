To Do List Manager

* Uses CRUD - Create, Read, Update, Delete Methodology

* Maintains an interactive, continous loop using 'While' and managing and tracking task states in real time

* Has fallback blocks such as Try/Except to handle variable and unpredictable user input and runtime crashes

* Utilizes dynamic zero-indexed logic to safely mark specific tasks as complete or to remove them from memory.